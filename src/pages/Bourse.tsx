import { CategoryPage } from '../components/assets/CategoryPage';

export default function Bourse() {
  return (
    <CategoryPage
      category="bourse"
      description="PEA, PEA-PME, compte-titres (CTO). Une ligne = un titre (action ou ETF)."
    />
  );
}
